kind: ApisixGlobalRule
metadata:
  name: vacuna-global-rule
  namespace: vacuna
spec:
  plugins:
    - name: request-id
      enable: true
      config:
        header_name: X-Request-Id
        include_in_response: true